//
//  CurrentSelections.swift
//  mobile application test
//
//  Created by Sam Isaacs on 25/03/2016.
//  Copyright © 2016 Sam Isaacs. All rights reserved.
//

import UIKit


/*TEST CODE TO CREATE CELL VARIABLES - NOT NEEDED.
 
 //SI. MARK: pc properties
 //SI. variables because they will change for each cell.
 var currentPC: Int
 var currentMon: Int
 
 
 //SI. set the initial value of each variable
 init?(currentPC: Int, currentMon: Int) {
 self.currentPC = currentPC
 self.currentMon = currentMon
 
 //SI. for validation an if statement has been
 if currentPC < 0 || currentMon < 0 {
 return nil
 }
 }
 
 */